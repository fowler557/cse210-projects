public class EternalGoal : Goal
{
    public EternalGoal(string goalName, string goalDescription, int goalNumber, string goalType, int goalPoints) : base(goalName, goalDescription, goalNumber, goalType,goalPoints)
    {
        
    }
}